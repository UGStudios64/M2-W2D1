using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exercise1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, World!"); // Print "Hello, World!" to the console when the game starts
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Frame per Frame"); // Print "Frame per Frame" to the console every frame
    }
}
